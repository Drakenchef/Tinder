server {
    listen 80;
    server_name theverest.ru www.theverest.ru;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
    
    location /images/ {
        alias /app/images/;
    }
}
